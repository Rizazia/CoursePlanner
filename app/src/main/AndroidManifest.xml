<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.courseplanner">

    <application
        android:name=".ChannelHandler"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SearchActivity" />
        <activity android:name=".OverviewActivity" />
        <activity android:name=".ItemListActivity" />
        <activity android:name=".CurrentCourseActivity" />
        <activity android:name=".AddModAssessmentActivity" />
        <activity android:name=".AddModMentorActivity" />
        <activity android:name=".AddModCourseActivity" />
        <activity android:name=".AddModTermActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".NotificationReceiver"
            android:enabled="true"
            android:exported="false" />
    </application>

</manifest>